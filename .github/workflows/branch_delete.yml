name: Branch Delete

on:
  delete

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: payload
      run: |
        echo "${{ github.event.ref }}"
        echo "${{ github.event.ref_type }}"
    
    - name: print
      run: env
